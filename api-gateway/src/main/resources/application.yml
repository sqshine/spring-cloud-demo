server:
  port: 8082

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
      - id: baidu_route
        uri: http://www.baidu.com:80/
        predicates:
        - Path=/hello